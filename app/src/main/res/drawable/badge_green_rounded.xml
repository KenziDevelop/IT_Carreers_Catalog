<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <solid android:color="#4CAF50" /> <!-- Hijau sukses (Material Green) -->

    <corners android:radius="24dp" /> <!-- Sudut membulat cukup besar biar terlihat seperti badge -->

    <!-- Optional: stroke tipis putih biar lebih pop -->
    <stroke
        android:width="1.5dp"
        android:color="#FFFFFF" />

    <!-- Padding dalam (opsional, bisa diatur di layout) -->
    <padding
        android:left="12dp"
        android:top="4dp"
        android:right="12dp"
        android:bottom="4dp" />

</shape>